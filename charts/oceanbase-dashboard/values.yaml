initCredentials: true
# base64 encoded password. If not set, the chart will generate it randomly
adminPassword: 

userCredentials: 
userNamespace: 
configNamespace: 

service:
  type: NodePort
  port: 80

# envs:
#   - name: "ODC_URL"
#     value: "http://oceanbase-dashboard:80"